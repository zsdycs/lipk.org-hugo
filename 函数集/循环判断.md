```

      _.find(this.cache.roleList, (e) => {
        if (e.checked === true) {
          _.find(this.formData.roleList, (v) => {
            if (v.value === e.value) {
              v.checked = true;
            }
          });
        }
      });
    // **************************
        this.cache.roleList.forEach((cache: any) => {
        if (cache.checked === true) {
          this.formData.roleList.forEach((element: any) => {
            if (element.value === cache.value) {
              element.checked = true;
            }
          });
        }
      });
```